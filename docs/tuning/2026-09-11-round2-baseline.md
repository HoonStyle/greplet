# 2차 튜닝 전 기준선 — 실제 재측정

측정: 2026-09-11 12:40 KST · 기준 구현: `4f72360` · 상태: **튜닝 전 600회 실행 완료, 튜닝 후 미실행**.

운영 배포 모듈을 별도 실행 디렉터리에 복사해 보존하고, 기존 40개 질문·정답으로 다시 검색했다. 운영 검색 모듈과 개발 빌드의 해시가 같은지 확인했다. 운영 설정·청크·모델·인덱스는 변경하지 않았다.

## 재측정 결과

| 모드 | 이전 측정 적중@5 | 이번 적중@5 | 이전 중앙값 | 이번 중앙값 | 이번 p95 |
|---|---:|---:|---:|---:|---:|
| FTS | 16/40 | 16/40 | 9.43ms | 8.93ms | 10.54ms |
| vector | 30/40 | 30/40 | 50.31ms | 48.85ms | 55.90ms |
| hybrid | 28/40 | 28/40 | 59.08ms | 57.80ms | 70.07ms |

**전 600회와 이번 600회의 상위 5개 ID·순서·점수가 모두 동일했다.** 모든 질문에서 5회 반복 순위가 안정적이었고, 검색 경고와 결과 캐시 적중은 0이었다. 구현 변경이 없는 재측정이므로 소폭 지연 차이를 튜닝 효과로 해석하지 않는다.

## 자연어 진단군 기준선

40개 중 기존 자연어 진단 13개와 기존 별도 평가 8개를 추려 집계했다. 이는 새 검증군이 아니며 실사용 전체 정확도를 뜻하지 않는다.

| 모드 | 목표 적중@5 | 중앙값 |
|---|---:|---:|
| FTS | 3/21 | 9.07ms |
| vector | 13/21 | 48.85ms |
| hybrid | 9/21 | 57.94ms |

vector만 적중한 4개와 두 모드 모두 놓친 8개가 재현됐다. 실패 분류는 [자연어 후속 분석](natural-language-followup.md)에 기록했다.

## 이후 비교 조건

- 같은 질문·정답 40개, 지정 워크스페이스, topN=5, 3모드 × 5회 = 600회.
- 결과 캐시 우회, 모드 순서 회전, 예열은 통계에서 제외.
- 모든 목표 청크가 존재하고 측정 전후 10개 워크스페이스의 매니페스트 해시가 그대로임을 확인했다.
- 원본 결과·질문·실행 모듈·벤치마크·측정 전 매니페스트 해시는 저장소 밖 로컬에 보존했다. 공개 데이터에는 원문 질문·함수명·경로를 넣지 않는다.
- 후속 튜닝 시 질문 해시와 인덱스 기준을 다시 확인한다. 달라졌다면 그 조건에서 기준선도 재측정한다.
- 지연은 로컬 함수 호출 시간이며 HTTP 전송·동시 부하를 제외한다. 튜닝 후 결과를 얻기 전에는 개선율을 계산하지 않는다.

공개 근거: [재측정 집계 JSON](results/2026-09-11-round2-baseline.json).
